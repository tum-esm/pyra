---
hide_table_of_contents: true
---

<!-- markdownlint-disable -->

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L0" className="SourceCodeLinkBadge">source code</a>

# <kbd>module</kbd> `core.types.config`






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L16" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictFilePath`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L19" className="SourceCodeLinkBadge">source code</a>

### <kbd>classmethod</kbd> `path_should_exist`

```python
path_should_exist(v: 'str', info: 'ValidationInfo') → str
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L30" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictDirectoryPath`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L33" className="SourceCodeLinkBadge">source code</a>

### <kbd>classmethod</kbd> `path_should_exist`

```python
path_should_exist(v: 'str', info: 'ValidationInfo') → str
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L44" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictIPAdress`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L50" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `TimeDict`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L55" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `as_datettime_time`

```python
as_datettime_time() → time
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L59" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialTimeDict`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L65" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `GeneralConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L73" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialGeneralConfig`
Like `SubConfigGeneral`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L84" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `OpusConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L93" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialOpusConfig`
Like `SubConfigOpus`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L104" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `CamtrackerConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L113" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialCamtrackerConfig`
Like `SubConfigCamtracker` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L124" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `ErrorEmailConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L134" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialErrorEmailConfig`
Like `SubConfigErrorEmail` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L146" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `MeasurementDecisionConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L152" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialMeasurementDecisionConfig`
Like `SubConfigMeasurementDecision` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L160" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `MeasurementTriggersConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L169" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialMeasurementTriggersConfig`
Like `SubConfigMeasurementTriggers` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L180" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `TumPlcConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L186" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialTumPlcConfig`
Like `SubConfigTumPlc`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L194" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `HeliosConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L202" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialHeliosConfig`
Like `SubConfigHelios`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L212" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `UploadStreamConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L222" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `UploadConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L229" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialUploadConfig`
Like `SubConfigUpload`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L238" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `Config`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L324" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `dump`

```python
dump(with_filelock: 'bool' = True) → None
```





---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L249" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `load`

```python
load(
    config_object: 'Optional[str | dict[Any, Any]]' = None,
    with_filelock: 'bool' = True,
    ignore_path_existence: 'bool' = False
) → Config
```

Load the config file. 



**Args:**
 
 - <b>`config_object`</b>:           If provided, the config file will be ignored and  the provided content will be used instead. Defaults  to None. 
 - <b>`with_filelock`</b>:           If True, the config file will be locked  during loading. Defaults to True. 
 - <b>`ignore_path_existence`</b>:   If True, the existence of the file and directory  paths used in the whole config file will not be  checked. Defaults to False. 

Returns:  The loaded config object. 



**Raises:**
 
 - <b>`ValueError`</b>:   If the config file is invalid. 


---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L334" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialConfig`
Like `ConfigDict`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L347" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `load`

```python
load(
    config_object: 'str',
    ignore_path_existence: 'bool' = False
) → PartialConfig
```

Load a partial config file. 



**Args:**
 
 - <b>`config_object`</b>:           JSON string containing a partial config. 
 - <b>`ignore_path_existence`</b>:   If True, the existence of the file and directory  paths used in the whole config file will not be  checked. Defaults to False. 

Returns:  The loaded partial config object. 



**Raises:**
 
 - <b>`ValueError`</b>:   If the config file is invalid. 


