---
hide_table_of_contents: true
---

<!-- markdownlint-disable -->

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L0" className="SourceCodeLinkBadge">source code</a>

# <kbd>module</kbd> `core.types.config`






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L17" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StricterBaseModel`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L21" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictFilePath`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L24" className="SourceCodeLinkBadge">source code</a>

### <kbd>classmethod</kbd> `path_should_exist`

```python
path_should_exist(v: 'str', info: 'ValidationInfo') → str
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L35" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictDirectoryPath`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L38" className="SourceCodeLinkBadge">source code</a>

### <kbd>classmethod</kbd> `path_should_exist`

```python
path_should_exist(v: 'str', info: 'ValidationInfo') → str
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L49" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `StrictIPAdress`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L55" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `TimeDict`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L60" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `as_datetime_time`

```python
as_datetime_time() → time
```






---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L64" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialTimeDict`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L70" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `GeneralConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L78" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialGeneralConfig`
Like `GeneralConfig`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L90" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `OpusConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L99" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialOpusConfig`
Like `OpusConfig`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L110" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `CamtrackerConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L120" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialCamtrackerConfig`
Like `CamtrackerConfig` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L132" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `ErrorEmailConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L142" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialErrorEmailConfig`
Like `ErrorEmailConfig` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L154" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `MeasurementDecisionConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L160" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialMeasurementDecisionConfig`
Like `MeasurementDecisionConfig` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L168" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `MeasurementTriggersConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L177" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialMeasurementTriggersConfig`
Like `MeasurementTriggersConfig` but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L188" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `TumPlcConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L194" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialTumPlcConfig`
Like `TumPlcConfig`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L202" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `HeliosConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L214" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialHeliosConfig`
Like `HeliosConfig`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L230" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `UploadStreamConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L240" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `UploadConfig`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L247" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialUploadConfig`
Like `UploadConfig`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 




---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L256" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `Config`





---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L338" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `dump`

```python
dump(with_filelock: 'bool' = True) → None
```





---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L267" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `load`

```python
load(
    config_object: 'Optional[str | dict[Any, Any]]' = None,
    with_filelock: 'bool' = True,
    ignore_path_existence: 'bool' = False
) → Config
```

Load the config file. 



**Args:**
 
 - <b>`config_object`</b>:           If provided, the config file will be ignored and  the provided content will be used instead. Defaults  to None. 
 - <b>`with_filelock`</b>:           If True, the config file will be locked  during loading. Defaults to True. 
 - <b>`ignore_path_existence`</b>:   If True, the existence of the file and directory  paths used in the whole config file will not be  checked. Defaults to False. 

Returns:  The loaded config object. 



**Raises:**
 
 - <b>`ValueError`</b>:   If the config file is invalid. 

---

<a href="https://github.com/tum-esm/pyra/tree/main/core/types/config/update_in_context#L347" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `update_in_context`

```python
update_in_context() → Generator[Config, None, None]
```

Update the confug file in a context manager. 



**Example:**
 

```python
with Config.update_in_context() as state:
     config.somesetting = somevalue
``` 

The file will be locked correctly, so that no other process can interfere with the config file and the state. 


---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L377" className="SourceCodeLinkBadge">source code</a>

## <kbd>class</kbd> `PartialConfig`
Like `Config`, but all fields are optional. 


---

#### <kbd>property</kbd> model_computed_fields

Get the computed fields of this model instance. 



**Returns:**
  A dictionary of computed field names and their corresponding `ComputedFieldInfo` objects. 

---

#### <kbd>property</kbd> model_extra

Get extra fields set during validation. 



**Returns:**
  A dictionary of extra fields, or `None` if `config.extra` is not set to `"allow"`. 

---

#### <kbd>property</kbd> model_fields_set

Returns the set of fields that have been explicitly set on this model instance. 



**Returns:**
  A set of strings representing the fields that have been set,  i.e. that were not filled from defaults. 



---

<a href="https://github.com/tum-esm/pyra/tree/main/packages/core/types/config.py#L390" className="SourceCodeLinkBadge">source code</a>

### <kbd>method</kbd> `load`

```python
load(
    config_object: 'str',
    ignore_path_existence: 'bool' = False
) → PartialConfig
```

Load a partial config file. 



**Args:**
 
 - <b>`config_object`</b>:           JSON string containing a partial config. 
 - <b>`ignore_path_existence`</b>:   If True, the existence of the file and directory  paths used in the whole config file will not be  checked. Defaults to False. 

Returns:  The loaded partial config object. 



**Raises:**
 
 - <b>`ValueError`</b>:   If the config file is invalid. 


