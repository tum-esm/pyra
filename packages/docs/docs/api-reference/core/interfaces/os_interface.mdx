---
hide_table_of_contents: true
---

<!-- markdownlint-disable -->

# <kbd>module</kbd> `core.interfaces.os_interface`






---

## <kbd>class</kbd> `OSInterface`







---

### <kbd>method</kbd> `get_process_status`

```python
get_process_status(process_name: str) → str
```

Takes a process name "*.exe" and returns its OS process status (see return types). 

---

### <kbd>method</kbd> `validate_disk_space`

```python
validate_disk_space() → None
```

Raises a `OSInterface.StorageError` the disk is more than 90% full 

---

### <kbd>method</kbd> `validate_system_battery`

```python
validate_system_battery() → None
```

Raises LowEnergyError if system battery runs lower than 20%. 


