const ConfigElementNote = (props: { children: React.ReactNode }) => (
    <div className="w-full pl-[13rem] text-xs text-slate-500 flex-row-left gap-x-1 flex-shrink-0">
        {props.children}
    </div>
);

export default ConfigElementNote;
