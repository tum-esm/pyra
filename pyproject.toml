[tool.poetry]
name = "pyra"
version = "4.0.0-alpha-1"
description = ""
authors = ["dostuffthatmatters <moritz@dostuffthatmatters.dev>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
pytest = "^7.0.1"
black = "^22.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
